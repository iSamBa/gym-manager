import { memo } from "react";

/**
 * MemberTableFilters - Placeholder component for table filtering
 *
 * Currently, filtering is handled at the page level via MemberFilters component.
 * This component is reserved for future table-level filter controls if needed.
 */
export const MemberTableFilters = memo(function MemberTableFilters() {
  // No filters currently implemented at table level
  // Filters are handled by parent page component
  return null;
});
