# Supabase Configuration
# Get these values from your Supabase project dashboard:
# https://supabase.com/dashboard/project/YOUR_PROJECT_ID/settings/api

# Supabase API URL (e.g., https://xxxxxxxxxxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url

# Supabase Anonymous Key (public key safe to expose in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Node Environment (development | production | test)
NODE_ENV=development

# Sentry Configuration (Error Tracking & Performance Monitoring)
# Get these values from your Sentry project dashboard:
# https://sentry.io/settings/YOUR_ORG/projects/YOUR_PROJECT/keys/

# Sentry DSN (Data Source Name) - public key for error reporting
# Optional in development, required in production
NEXT_PUBLIC_SENTRY_DSN=

# Sentry Organization slug (required for source map uploads)
SENTRY_ORG=

# Sentry Project slug (required for source map uploads)
SENTRY_PROJECT=

# Sentry Auth Token (required for source map uploads in CI/CD)
# Create at: https://sentry.io/settings/account/api/auth-tokens/
# Requires scopes: project:releases, org:read
SENTRY_AUTH_TOKEN=
